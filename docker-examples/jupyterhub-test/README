
### Launching Jupyter Hub through docker container using DockerSpawner ###

# Docker Compose Commands

```
// build with no cache
docker-compose build --no-cache
// start the services
docker-compose up
// list the services
docker-compose ps
// list the containers
docker ps
// stop services
docker-compose stop
```

# Access Jupyterhub

localhost:8000

# Notes: Works fineS
